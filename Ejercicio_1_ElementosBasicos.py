# Escribir la siguiente expresión en forma de expresión algoritmica (Checar la expresion en la imagen en la carpeta Referencias del Proyecto)

a = float(input("a = "))
b = float(input("b = "))
c = float(input("c = "))

resultado = (a**3 * (b**2 - 2*a*c)) / (2*b)

print(f"El resultado es : {resultado}")

# Se pueden trabajar con valores tanto enteros como flotantes, al momento de escribir los valores de las variables en la consola